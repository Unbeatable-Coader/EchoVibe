<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Your Social Media App</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f5f5f5;
      margin: 0;
      padding: 0;
    }

    header {
      background-color: #1DA1F2;
      color: white;
      padding: 10px;
      text-align: center;
      height: 400px;
    }

    #logo {
      padding-bottom: 30px;
      max-height: 100%;
    }

    #container {
      max-width: 600px;
      margin: 20px auto;
      background-color: white;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    #signup, #login {
      padding: 10px;
      margin: 10px 0;
      width: 100%;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      font-size: 16px;
    }

    #signup {
      background-color: #4CAF50;
      color: white;
    }

    #login {
      
      background-color: #3498db;
      color: white;
    }
    .button-style {
    display: inline-block;
    padding: 10px;
    margin: 10px;
    background-color: #3498db;
    color: white;
    text-decoration: none;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    font-size: 16px;
  }
  .green-button-style {
    display: inline-block;
    padding: 10px;
    margin: 10px;
    background-color: #4CAF50; /* Green color */
    color: white;
    text-decoration: none;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    font-size: 16px;
  }


  </style>
</head>
<body>
  <header style ="display: flex">
    <div>
    <img id="logo" src="/assets/EchoVibe_logo.png" alt="Your App Logo">
    </div>

    <div style= "text-align: center; align-item: center">
    <h2 style="line-height:350px; margin-left:50px; font-size:40px;">Share Moments, Connect Lives - Your Story, Our Echo</h2>
    </div>
  </header>

  <div id="container">
    <% if user_signed_in? %>
        <%= button_to "Sign Out", destroy_user_session_path, method: :delete %>
    <% else %>
        <%= link_to 'Log In', new_user_session_path, class: 'green-button-style' %>
        <%= link_to 'Sign Up', new_user_registration_path, class: 'button-style float-end' %>
    <% end %>

  </div>
<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>
</body>
</html>
